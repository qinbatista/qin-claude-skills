# Task Lifecycle

Merge this section into `~/.claude/CLAUDE.md`.

Score every submission 0-100 before task files: small 0-24,standard 25-49,complex 50-74,advanced 75-100;show `Complexity:N/100 (band)` and route change in every result.Eligible text/code:pipe exact user text once non-TTY to `/usr/bin/python3 ~/.claude/skills/task-analyze-skill/scripts/obsidian_adaptive_model_runner.py` before skills/memory/files;wait 60s,resume session once <=60s,return result/pending;NEVER spawn/read/re-enter.Exact one-source/tool/image:use `task_complexity_score.py`,then direct action/read,no reread/full read/precheck.Small low-risk edit score<=24:Haiku-low first;Ending quality FAIL suppresses Haiku for matching project+task+operation+code+band and next task upgrades to saved quality pair.Otherwise saved ladder:2 Real PASS down 1 rung;quality FAIL up 1;zero-result failure one stronger fallback.Missing Obsidian uses saved cold start,no block.2-3 independent read-only sources cost-admit before reads;dependent multi-file one producer.Producer owns files/skills/Mini Test;heavy/API/large/side-effect checks syntax+names/references.End Task hard-required and BLOCKING:after result write scored local ledger,then run Ending in the SAME turn with Agent(run_in_background:false,ENDING_TASK_WORKER) and wait for its verdict;verification-needed work uses `ending_verification_plan.py`,one own scored/model End Task per independent real test/API/render,and all checks must PASS.Detached background is allowed ONLY for a skill's own declared verify step,never for the general Ending lifecycle.NEVER report done/complete/ready,and never hand the turn back for the next step,while any required check is unrun,pending,or FAILing;say `Ending:IN PROGRESS - not done` plus the outstanding check and keep working.Showing the result is a progress presentation,not a done claim.FAIL creates Fix Task with exact error then fresh End Task,up to 3 repairs;BLOCKED only unavailable/external/limit;never emulate or self-verify - the verifier is always a different agent/model from the producer.Terminal events sync local history+Obsidian Claude Model Switch.No hook.Final PASS/BLOCKED Ending-only,reported in the same turn as the result.Benchmark 3 tiers from `fable|max`;Direct fixed/no verify;Auto receipt=child/graph;compare task vs task+Ending,controller excluded.
